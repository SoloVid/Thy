
export const callAssignTs = `
// Call a function imperatively
console.log("himom")

// Assign function output (to constant)
const o = JSON.parse("{}")

// Create mutable variable
let i = getInitialValue()
// Re-assign variable
i = i + 1
`

export const callAssignThy = `
Call a function imperatively
print "himom"

Assign function output (to constant)
o is json.decode "{}"

Create mutable variable
i be getInitialValue
Re-assign variable
i to math.add i 1
`
