
export const ifTs = `
if (a === b) {
  console.log("yes")
}
const c = a < b ? "less" : "more"
`

export const ifThy = `
check.equal a b
if that
  print "yes"
check.asc a b
c is if that
  return "less"
and else
  return "more"
`
