```thy
a is given Number
b is given Number

type return Number
```

Calculate the remainder of `a` and `b` (i.e. `a % b`) and return the result.

**Example:**

```thy
math.mod 10 3
Print 1
print that
```
