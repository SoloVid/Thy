```thy
b is given Number
x is given Number

type return Number
```

Calculate the `b` raised to the power of `x` (i.e. `b ^ x`) and return the result.

**Example:**

```thy
math.pow 2 3
Print 8
print that
```
